# OSC-to-DMX

This repository contains a project that enables control of DMX (Digital Multiplex) devices via OSC messages. The application receives an array of DMX values as OSC and relays them out the serial port to an Enttec device.